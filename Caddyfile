# Caddy obtiene y renueva el certificado HTTPS (Let's Encrypt) automáticamente.
# Requiere los puertos 80 y 443 abiertos en el firewall del VPS y que
# brewidea.duckdns.org resuelva a la IP de este VPS.
brewidea.duckdns.org {
	encode gzip
	reverse_proxy app:3000
}
